import React from "react";

const QuestionHeader = ({round, category, type, timeLeft}) => {
   return (
       <div className="question-header">

       </div>
   )
};

export default QuestionHeader;
