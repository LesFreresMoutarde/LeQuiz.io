import React from "react";

const LobbyValue = ({value}) => {
    return (
        <div className="lobby-value-wrapper">
            <p className="lobby-value">{value}</p>
        </div>
    )
};

export default LobbyValue;
